# 开发计划：Novel Agent → 双击 exe 桌面应用

## 目标

将当前依赖 Docker Compose + Dify + Python/Node 运行时的 Web 应用，改造成**单个 `novel_agent.exe`，双击启动，系统浏览器打开即用**的桌面应用。

## 现状分析

```
当前架构                          exe 化后
─────────────────────────────────────────────────────
React/Vite 前端 (30 文件, ~300K) → 构建产物由 Rust/Tauri serve
Flask 后端 (12K 行 Python)       → Rust (Tauri Command) + Python sidecar
Dify Agent 工作流 (3 活跃)       → LangGraph (直调 LLM API)
Docker Compose 编排              → 删除
```

### Dify 工作流复杂度（好消息：非常简单）

| Agent | 节点数 | 结构 |
|-------|--------|------|
| 续写 Agent | 3 | start → LLM → answer |
| 审核 Agent | 3 | start → LLM → answer |
| 灵感大纲 Agent | 5 | start → 2×LLM → 2×answer |

每个 Agent 本质是：**系统提示词 + 工具调用 + LLM 生成**。Dify 相当于做了 HTTP 转发，没有复杂编排逻辑。迁移到 LangGraph 的工作量远小于最初估计。

### 已有基础设施（可直接复用）

- `utils/model_provider.py` — DeepSeek / OpenAI 兼容双通道，已支持多个 API key
- `pipelines/` — 6 个 LangChain 管线已经绕开 Dify 直调 LLM
- `utils/dify_client.py` — 可改为 LLM 直调客户端
- 前端 React 组件 30 个，基本不需要改

---

## 阶段一：去掉 Dify，Agent 直调 LLM（2 周）

这是整个计划的关键路径。做完后系统不再依赖 Dify 服务。

### 1.1 新建 Agent 引擎层（3 天）

```
novel_git_server/
└── engine/                    # 新增
    ├── __init__.py
    ├── base.py                # Agent 基类（prompt + tools + LLM 调用）
    ├── continuation.py        # 续写 Agent
    ├── review.py              # 审核 Agent
    └── outline.py             # 灵感大纲 Agent
```

`engine/base.py` 核心逻辑：

```python
class BaseAgent:
    system_prompt: str           # 从 Dify YAML 提取
    tools: list[Tool]            # 对应 Dify 工具节点
    model_config: BatchModelConfig

    def run(self, query: str, inputs: dict) -> Iterator[str]:
        # LangGraph: agent_llm → tool_node → agent_llm → output
        # 等价于 Dify 的 start → LLM → tool → LLM → answer
```

### 1.2 提取 Dify 工作流的系统提示词 + 工具配置（2 天）

从 `dify_workflows/` 的 YAML 中提取每个 Agent 的：
- 系统提示词（`sys.prompt` 或 `prompt_template`）
- 工具列表（`tool_call` 节点中的 `provider_type: loregittools`）
- 对话变量和输入 Schema

### 1.3 适配现有 API 路由（3 天）

当前 `app.py` 里 `/api/world/deduce_stream` 等路由调用 `dify_client.py` → Dify。改为调用 `engine/` → 直调 LLM。

```python
# 改动前
from utils.dify_client import run_dify_stream
# 改动后
from engine.continuation import ContinuationAgent
```

### 1.4 删除 Dify 依赖（1 天）

- 删除 `dify_workflows/` 中已退役的工作流
- 删除 `docker-compose.*.yml` 中 Dify/PostgreSQL/Redis 容器
- 清理 5 个 Dify API key 环境变量，统一为 `LLM_API_KEY`
- 卸载 `utils/dify_client.py` 和 `utils/dify_registry.py`

### 1.5 全链路测试（1 天）

用现有测试套件 + demo 脚本验证：
- 导入 → 摘要归档 → 世界观初始化 → 大纲 → 续写 → 审核 → 归档

---

## 阶段二：Tauri 重写（3 周）

### 2.1 初始化 Tauri 项目（1 天）

```
npx create-tauri-app novel_agent_desktop --template react-ts
```

在现有仓库中新建 `desktop/` 目录，不破坏现有代码。

### 2.2 迁移 Rust 层（核心工作，7 天）

将 Flask 的 17 个 route handler 重写为 Tauri Command：

```rust
// 每个 Tauri Command 替代一个 Flask route
#[tauri::command]
fn read_book_file(book_id: String, file_name: String) -> Result<String, String> {
    // 对应 GET /books/get_file
}
```

按优先级分批：

**第一批（基础 IO，2 天）**
| Flask Route | Tauri Command | 行数参考 |
|-------------|---------------|---------|
| `/books/init` | `init_book` | ~50 |
| `/books/search` | `search_books` | ~40 |
| `/books/get_file` | `read_book_file` | ~60 |
| `/books/update_file` | `write_book_file` | ~80 |
| `/books/history` | `get_book_history` | ~30 |
| `/checkout` | `checkout_context` | ~60 |

**第二批（Agent 触发，2 天）**
| `/api/world/deduce_stream` | `deduce_world_stream` | SSE → Tauri Event |
| `/api/draft/sync_all` | `draft_sync` | ~80 |
| `/api/draft/confirm` | `draft_confirm` | ~50 |
| `/api/draft/rollback` | `draft_rollback` | ~50 |
| `/api/rolling/*` | 对应 commands | ~100 |

**第三批（工具 + 番茄导入，2 天）**
| `/tools/*` | 对应 commands | ~200 |
| `/api/tomato/*` | 对应 commands | ~200 |

**第四批（Git 操作，1 天）**
| `/git/*` | `git_log`, `git_diff`, `git_branch` 等 | ~200 |

### 2.3 Python Sidecar 集成（3 天）

Rust 不重写 LangChain 管线（150K 行 Python），而是作为 sidecar 进程：

```
Tauri Rust ──stdin/stdout JSON-RPC──→ Python sidecar
                                           ├── pipelines/summary_archive.py
                                           ├── pipelines/world_model_init.py
                                           ├── pipelines/style_artifact_init.py
                                           ├── pipelines/rolling_chapter.py
                                           └── engine/ (Agent 工作流)
```

```rust
// Rust 侧
#[tauri::command]
async fn run_summary_pipeline(book_id: String) -> Result<String, String> {
    let sidecar = tauri::api::process::Command::new_sidecar("python-sidecar")
        .args(["summary_archive", &book_id])
        .output()
        .await?;
    Ok(sidecar.stdout)
}
```

Python sidecar 用 `pyembedded` 打包（Python 3.11 embedded，~50MB），不依赖系统 Python。

### 2.4 前端适配（4 天）

主要改动：`fetch()` → `invoke()`

**改动量估算**：

| 文件 | 当前调用方式 | 改动 |
|------|-------------|------|
| `hooks/useAgentSession.ts` (16K) | SSE fetch → state | `listen()` Tauri Event |
| `hooks/useDraftReview.ts` (19K) | REST fetch | `invoke()` |
| `store/index.ts` (40K) | REST fetch | `invoke()` |
| `api/` 目录 | axios/fetch 封装 | 改为 Tauri invoke 封装 |
| 各组件中的 fetch | 直接 fetch | `invoke()` |

前端 React 组件本身不需要改。

### 2.5 打包配置（2 天）

- Tauri `tauri.conf.json` 配置窗口、图标、权限
- Python embedded 打包到 `src-tauri/binaries/`
- Git portable 打包到 `src-tauri/binaries/`
- Windows NSIS/MSI 安装包配置
- 自动更新配置

### 2.6 端到端测试 + 修 bug（2 天）

---

## 阶段三：体验打磨（1 周）

### 3.1 桌面体验（2 天）
- 系统托盘、最小化到托盘
- 开机自启动选项
- 文件关联（`.novel` 项目文件双击打开）
- 窗口大小记忆

### 3.2 首次使用引导（2 天）
- 第一步：配置 LLM API key（支持 DeepSeek / OpenAI 兼容）
- 第二步：导入小说或创建新书
- 第三步：进入创作工作台
- 空状态引导页（替换当前空白页）

### 3.3 错误处理 + 日志（1 天）
- Python sidecar 崩溃自动重启
- LLM API 调用失败友好提示
- 本地日志文件（`%APPDATA%/novel_agent/logs/`）

---

## 总时间线

```
Week 1-2:  阶段一（Dify 替换）         ← 关键路径
Week 3-5:  阶段二（Tauri 重写）         ← 最大工作量
Week 6:    阶段三（体验打磨）
```

**总计：6 周**

## 风险与缓解

| 风险 | 影响 | 缓解 |
|------|------|------|
| Dify 提示词嵌入在 YAML 深层，提取遗漏 | 续写/审核质量下降 | 保留 YAML 原文对照，跑回归测试 |
| Python sidecar 与 Rust 通信不稳定 | 管线中断 | JSON-RPC + 超时重试 + 崩溃自启 |
| Tauri webview 兼容性 | 前端组件渲染异常 | 阶段二初期先验证 Monaco Editor 和 ReactFlow |
| Git portable 捆绑授权问题 | 无法发布 | 使用 `git2-rs` 纯 Rust 替代 |
| 前端 SSE 迁移到 Tauri Event | 流式体验断裂 | 阶段二早期单独验证 SSE → Event 转换 |

## 第一阶段可立即开始

阶段一不需要 Tauri，在当前 Python + Flask 环境里就能做。建议今天就：

1. 在 `novel_git_server/` 下新建 `engine/` 目录
2. 导出 `续写agent.yml` 里的系统提示词和工具列表
3. 用 `model_provider.py` 的 `create_chat_model()` 写第一个直调 LLM 的 Agent
4. 跑通"续写"链路的第一个版本
